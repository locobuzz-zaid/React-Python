import styled from "styled-components";

const AppWrapper = styled.div`
  .alert {
    position: fixed;
    bottom: 30px;
    right: 30px;
  }
  .navbar {
    .nav-link {
      .btn-secondary {
        padding: 0;
        background: none;
        border: none;
      }
    }
  }
`;

export default AppWrapper;
